{
  "name": "esbuild-course",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./src/index.js --bundle --outdir=\"build\" --minify",
    "dev": "cross-env MODE=development node .\\config\\build\\esbuild-config.js",
    "prod": "cross-env MODE=production node ./config/build/esbuild-config.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "esbuild": "^0.15.11"
  }
}
